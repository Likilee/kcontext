name: E2E CLI Integration (Nightly)

on:
  schedule:
    - cron: "0 18 * * *"
  workflow_dispatch:

jobs:
  real-data-integration:
    name: "Web: Fixture-backed CLI integration"
    uses: ./.github/workflows/reusable-web-e2e-real.yml
